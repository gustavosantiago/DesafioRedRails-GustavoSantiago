<%= form_for(current_user.user_followers.build) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit t('buttons.follow'), class: "btn btn-primary pull-right" %>
<% end %>